---
phase: 03-schedule-on-off
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pi-setup.md
autonomous: true
requirements:
  - SCHED-02
  - SCHED-03

must_haves:
  truths:
    - "pi-setup.md documents all four new cron entries with exact syntax ready to paste"
    - "The 7 AM on-entry (SCHED-02) is documented with the pkill restart pattern"
    - "All three off-entries (SCHED-03) are documented with correct day masks and times"
    - "The guide tells the user to copy stop_weather.sh to the Pi and make it executable before adding cron entries"
  artifacts:
    - path: "pi-setup.md"
      provides: "Cron setup instructions for on/off schedule"
      contains: "0 7 * * *"
  key_links:
    - from: "pi-setup.md step 9"
      to: "javi's crontab on the Pi"
      via: "user runs crontab -e and pastes the four entries"
      pattern: "crontab -e"
---

<objective>
Update pi-setup.md with a new section documenting all four Phase 3 cron entries: one 7 AM daily restart (SCHED-02) and three off-schedule entries (SCHED-03). Also instruct the user to copy stop_weather.sh to the Pi before configuring cron.

Purpose: The user manages cron manually via `crontab -e` on the Pi. This section gives exact, paste-ready cron lines so there is no ambiguity.

Output:
- pi-setup.md — gains a new "Step 9: Schedule On/Off (cron)" section appended after the existing Step 8 (auto-start cron section)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-schedule-on-off/03-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Schedule On/Off section to pi-setup.md</name>
  <files>pi-setup.md</files>

  <read_first>
    pi-setup.md — read the full file before editing. The new section appends after the
    existing Step 8 (auto-start cron). Know the exact wording of Step 8 so the new Step 9
    follows the same heading and code-block style.
    .planning/phases/03-schedule-on-off/03-CONTEXT.md — D-03, D-08, D-09, D-11 define
    the exact cron lines and filenames.
  </read_first>

  <action>
Append a new section to pi-setup.md immediately after the Step 8 block (the @reboot cron
section). Do not modify any existing content. Add:

---

### 9. Copy stop_weather.sh to the Pi

```bash
scp stop_weather.sh javi@map.local:/home/javi/stop_weather.sh
ssh javi@map.local "chmod +x /home/javi/stop_weather.sh"
```

Verify it's executable:
```bash
ssh javi@map.local "ls -la ~/stop_weather.sh"
```

### 10. Wire Up the On/Off Schedule (cron)

Open javi's crontab on the Pi:
```bash
ssh javi@map.local "crontab -e"
```

Add all four entries below the existing `@reboot` line:

```
# 7 AM daily restart (kill any running instance, then start fresh)
0 7 * * * pkill -f weather_lights.py 2>/dev/null; sleep 2; /home/javi/start_weather.sh >> /home/javi/weather_lights.log 2>&1

# Off schedule — weekdays
0 9 * * 1,2,4,5 /home/javi/stop_weather.sh >> /home/javi/weather_lights.log 2>&1

# Off schedule — Wednesday
0 16 * * 3 /home/javi/stop_weather.sh >> /home/javi/weather_lights.log 2>&1

# Off schedule — weekend
0 18 * * 6,0 /home/javi/stop_weather.sh >> /home/javi/weather_lights.log 2>&1
```

Save and exit. Verify all five entries are present (1 reboot + 4 schedule):
```bash
ssh javi@map.local "crontab -l"
```

Expected output includes:
- `@reboot /home/javi/start_weather.sh >> /home/javi/weather_lights.log 2>&1`
- `0 7 * * * pkill -f weather_lights.py 2>/dev/null; sleep 2; /home/javi/start_weather.sh >> /home/javi/weather_lights.log 2>&1`
- `0 9 * * 1,2,4,5 /home/javi/stop_weather.sh >> /home/javi/weather_lights.log 2>&1`
- `0 16 * * 3 /home/javi/stop_weather.sh >> /home/javi/weather_lights.log 2>&1`
- `0 18 * * 6,0 /home/javi/stop_weather.sh >> /home/javi/weather_lights.log 2>&1`

#### Schedule Reference

| Day(s) | Strip OFF at |
|--------|-------------|
| Mon, Tue, Thu, Fri | 9:00 AM |
| Wednesday | 4:00 PM |
| Saturday, Sunday | 6:00 PM |

The 7 AM entry always restarts — if the strip was already on from `@reboot`, it kills and
relaunches to ensure a clean process at the start of each day.

---

After adding cron entries, confirm the schedule is working by tailing the log around
the scheduled time:
```bash
ssh javi@map.local "tail -f ~/weather_lights.log"
```
  </action>

  <verify>
    <automated>
      grep -n "0 7 \* \* \*" pi-setup.md
      grep -n "pkill -f weather_lights.py 2>/dev/null" pi-setup.md
      grep -n "0 9 \* \* 1,2,4,5" pi-setup.md
      grep -n "0 16 \* \* 3" pi-setup.md
      grep -n "0 18 \* \* 6,0" pi-setup.md
      grep -n "stop_weather.sh" pi-setup.md
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep "0 7 \* \* \*" pi-setup.md` returns exactly one line containing the 7 AM on-entry (per D-03)
    - `grep "pkill -f weather_lights.py 2>/dev/null" pi-setup.md` returns at least one line
    - `grep "0 9 \* \* 1,2,4,5" pi-setup.md` returns exactly one line (Mon/Tue/Thu/Fri 9 AM off)
    - `grep "0 16 \* \* 3" pi-setup.md` returns exactly one line (Wed 4 PM off)
    - `grep "0 18 \* \* 6,0" pi-setup.md` returns exactly one line (Sat/Sun 6 PM off)
    - `grep "stop_weather.sh" pi-setup.md` returns at least 5 lines (scp, chmod, cron entries, expected output)
    - `grep "sleep 2" pi-setup.md` returns exactly one line (the 2-second gap in the 7 AM entry per D-01)
    - Existing Step 8 content is unchanged — `grep "@reboot" pi-setup.md` still returns the original @reboot cron line
  </acceptance_criteria>

  <done>pi-setup.md has a Step 9 (copy stop_weather.sh) and Step 10 (cron entries) section with all four exact cron lines as defined in D-03 and D-08, plus the schedule reference table and verification commands</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → user | User copies cron lines from pi-setup.md into crontab manually |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | Incorrect cron syntax entered by user | accept | pi-setup.md provides exact paste-ready lines and a `crontab -l` verification step; malformed cron entries silently fail but do not break existing entries |
| T-03-05 | Denial of Service | 7 AM pkill fires before @reboot script is fully initialized | accept | 2-second sleep after pkill (D-01) provides sufficient gap; worst case is a brief gap before start_weather.sh relaunches |
</threat_model>

<verification>
After the task completes, run locally:

```bash
grep "0 7 \* \* \*" pi-setup.md
grep "0 9 \* \* 1,2,4,5" pi-setup.md
grep "0 16 \* \* 3" pi-setup.md
grep "0 18 \* \* 6,0" pi-setup.md
grep "stop_weather.sh" pi-setup.md | wc -l
grep "@reboot" pi-setup.md
```

All six checks must return expected results.
</verification>

<success_criteria>
- pi-setup.md contains all four new cron entries with exact syntax from D-03 and D-08
- The 7 AM on-entry includes pkill, sleep 2, and start_weather.sh call
- All three off-entries call stop_weather.sh with correct day masks
- Instructions tell user to copy stop_weather.sh to Pi and chmod +x before cron setup
- Existing @reboot entry and Steps 1-8 are not modified
- Schedule reference table is present (Mon/Tue/Thu/Fri → 9 AM; Wed → 4 PM; Sat/Sun → 6 PM)
</success_criteria>

<output>
After completion, create `.planning/phases/03-schedule-on-off/03-02-SUMMARY.md` using the summary template.
</output>
