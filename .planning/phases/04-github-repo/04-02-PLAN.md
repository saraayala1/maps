---
phase: 04-github-repo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-github-repo/04-DEFERRED.md
autonomous: true
requirements:
  - REPO-03
  - REPO-04
must_haves:
  truths:
    - "REPO-03 and REPO-04 are formally recorded as deferred backlog items with enough detail to pick up later"
    - "The deferred record documents what each item requires so future planning does not need to re-derive from scratch"
  artifacts:
    - path: ".planning/phases/04-github-repo/04-DEFERRED.md"
      provides: "Formal backlog record for REPO-03 and REPO-04"
      contains: "REPO-03"
  key_links: []
---

<objective>
Record REPO-03 (test scripts per temperature zone and condition) and REPO-04 (manual simulation script) as formal deferred backlog items. No code is written. This plan satisfies the requirement that all four REPO-XX IDs appear in at least one plan and provides a concrete pickup point for a future pass.

Purpose: User explicitly deferred these per D-02: "REPO-03 and REPO-04 are deferred as chores for a later pass — they do NOT block Phase 4 completion." This plan acknowledges them formally so they are not lost.

Output: .planning/phases/04-github-repo/04-DEFERRED.md containing the backlog specification for both items.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/04-github-repo/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write deferred backlog record for REPO-03 and REPO-04</name>
  <files>.planning/phases/04-github-repo/04-DEFERRED.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md — read REPO-03 and REPO-04 definitions verbatim
    - .planning/phases/04-github-repo/04-CONTEXT.md — read the `<deferred>` section for user's stated intent
  </read_first>
  <action>
    Create .planning/phases/04-github-repo/04-DEFERRED.md with the following content. This is a planning artifact, not code — write it exactly as specified:

    ```markdown
    # Phase 4 — Deferred Backlog

    Deferred by user on 2026-05-25 per D-02 in 04-CONTEXT.md.
    These items do not block Phase 4 completion.

    ---

    ## REPO-03: Test Scripts (per condition and zone)

    **Requirement (from REQUIREMENTS.md):**
    Test scripts covering: each temperature zone color, each weather condition animation, schedule on/off behavior.

    **What this means in practice:**
    - A script that sets strip to each of the 6 temperature zone colors (< 60°F / 60–70°F / 70–80°F / 80–90°F / 90–99°F / 100°F+) in sequence, holding each for 2–3 seconds so the user can visually confirm the color is correct
    - A script that cycles through the 3 condition animations: rain pulse (blue), storm (blue + yellow flash), wind breathe — each held for 5–10 seconds
    - A script or manual cron test that confirms start_weather.sh and stop_weather.sh are invocable by cron

    **Files it would produce:**
    - `test_zones.py` — temperature zone color test
    - `test_conditions.py` — condition animation test

    **When to pick up:** After Phase 3 is verified on the Pi in live use; run as a standalone chore, no new phase needed.

    ---

    ## REPO-04: Manual Simulation Script

    **Requirement (from REQUIREMENTS.md):**
    Manual simulation test script — accepts a temperature (°F) and boolean flags for each condition (rain, storm, clear, wind) and drives the strip exactly as the live script would, without fetching any API data.

    **What this means in practice:**
    - CLI script: `python3 simulate.py --temp 85 --rain --wind`
    - Calls the same animation functions as weather_lights.py (import them directly or copy the logic)
    - No API call is made; arguments replace the weather fetch result
    - Useful for verifying strip behavior without waiting for matching real-world weather

    **Files it would produce:**
    - `simulate.py` — simulation entry point

    **When to pick up:** After REPO-03 test scripts are done; natural follow-on. Requires weather_lights.py to expose its animation functions as importable (or a small refactor to split logic into a module).

    ---

    ## STATUS-01: Status File (also deferred — D-03)

    **Requirement:** On each weather fetch loop, write a one-line status to /home/javi/weather_status.

    **When to pick up:** Small addition to weather_lights.py main loop. Can be added in a single PR when convenient.
    ```

    After creating the file, do not commit it — leave it as an untracked planning artifact.
  </action>
  <verify>
    <automated>grep -c "REPO-03\|REPO-04" /Users/sayala/Documents/Pi/.planning/phases/04-github-repo/04-DEFERRED.md</automated>
    Must return 2 or more.
  </verify>
  <acceptance_criteria>
    - File exists at `/Users/sayala/Documents/Pi/.planning/phases/04-github-repo/04-DEFERRED.md`
    - `grep "REPO-03" /Users/sayala/Documents/Pi/.planning/phases/04-github-repo/04-DEFERRED.md` returns a match
    - `grep "REPO-04" /Users/sayala/Documents/Pi/.planning/phases/04-github-repo/04-DEFERRED.md` returns a match
    - `grep "simulate.py" /Users/sayala/Documents/Pi/.planning/phases/04-github-repo/04-DEFERRED.md` returns a match
    - `grep "test_zones.py" /Users/sayala/Documents/Pi/.planning/phases/04-github-repo/04-DEFERRED.md` returns a match
    - File is NOT staged or committed (it is a planning artifact, not production code)
  </acceptance_criteria>
  <done>04-DEFERRED.md exists with formal backlog specs for REPO-03, REPO-04, and STATUS-01; REPO-03 and REPO-04 are formally acknowledged as deferred</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning docs → git index | Planning artifacts (04-DEFERRED.md) should not be committed to the production repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Information Disclosure | 04-DEFERRED.md in git | accept | File contains no secrets — only backlog descriptions; acceptable to leave untracked or commit to .planning/ |
| T-04-07 | Repudiation | deferred items forgotten | mitigate | Formal written record in 04-DEFERRED.md ensures items are not silently dropped; user can reference at any time |
</threat_model>

<verification>
After task completes:

1. `ls /Users/sayala/Documents/Pi/.planning/phases/04-github-repo/` — must show 04-DEFERRED.md
2. `grep -c "REPO-0[34]" /Users/sayala/Documents/Pi/.planning/phases/04-github-repo/04-DEFERRED.md` — must return 2 or more
3. Confirm no secrets in file: `grep -i "api.key\|weatherbit\|password\|token" /Users/sayala/Documents/Pi/.planning/phases/04-github-repo/04-DEFERRED.md | wc -l` — must return 0
</verification>

<success_criteria>
- 04-DEFERRED.md exists with concrete pickup specs for REPO-03 and REPO-04
- REPO-03 and REPO-04 are formally acknowledged as deferred (not silently dropped)
- No production code was written or modified in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/04-github-repo/04-02-SUMMARY.md` using the summary template.
</output>
