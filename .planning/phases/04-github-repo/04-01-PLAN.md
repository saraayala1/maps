---
phase: 04-github-repo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
autonomous: true
requirements:
  - REPO-01
  - REPO-02
must_haves:
  truths:
    - "The repo at github.com/saraayala1/maps is up to date with all production files"
    - "No .env file (API key) appears in the GitHub repo at any commit"
    - "README.md is present and publicly readable on GitHub"
    - "pi-setup.md is present and publicly readable on GitHub"
    - ".gitignore excludes .env and weather_lights.log before the first push"
  artifacts:
    - path: ".gitignore"
      provides: "Prevents .env and log files from ever being committed"
      contains: ".env"
    - path: "weather_lights.py"
      provides: "Main production script — confirmed present in remote repo"
    - path: "start_weather.sh"
      provides: "Boot wrapper — confirmed present in remote repo"
    - path: "stop_weather.sh"
      provides: "Stop wrapper — confirmed present in remote repo"
    - path: "README.md"
      provides: "Project description + setup pointer — confirmed present in remote repo"
    - path: "pi-setup.md"
      provides: "Canonical 10-step Pi setup guide — confirmed present in remote repo"
  key_links:
    - from: "local main branch"
      to: "origin/main (github.com/saraayala1/maps)"
      via: "git push origin main"
      pattern: "git push"
---

<objective>
Publish all production files to github.com/saraayala1/maps by creating a .gitignore that guards the API key, then pushing the local branch that is already 3 commits ahead of origin.

Purpose: Satisfies REPO-01 (all code on GitHub) and REPO-02 (README + pi-setup.md publicly accessible). No new code is written — this is a publish operation on files that already exist locally.

Output: github.com/saraayala1/maps/tree/main contains weather_lights.py, start_weather.sh, stop_weather.sh, README.md, pi-setup.md, and .gitignore. Repo is public.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-github-repo/04-CONTEXT.md

<interfaces>
<!-- Key facts about the repo state the executor must know. -->
<!-- Verified from git status + git remote output during planning. -->

Current git state:
- Branch: main
- Remote: origin → https://github.com/saraayala1/maps.git
- Status: 3 commits ahead of origin/main (all production changes already committed locally)
- Untracked: .planning/ SUMMARY files and 02-boot-auto-start planning docs (leave these untracked for now — only production files matter for this push)
- Modified (not staged): .claude/settings.local.json, .planning/PROJECT.md, .planning/REQUIREMENTS.md (do NOT stage these)

Production files already committed in local HEAD:
- weather_lights.py
- start_weather.sh
- stop_weather.sh
- README.md
- pi-setup.md

.gitignore: does NOT exist yet — must be created before push.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .gitignore to guard secrets</name>
  <files>.gitignore</files>
  <read_first>
    - Run `git status` to confirm current tracked/untracked state
    - Run `ls -la /Users/sayala/Documents/Pi/.env 2>/dev/null` to confirm .env is not already tracked
    - Run `git ls-files --error-unmatch .env 2>/dev/null || echo "not tracked"` to verify .env is untracked
  </read_first>
  <action>
    Create .gitignore at the repo root with the following exact content (per D-Claude's discretion to add .gitignore; per CONTEXT.md specifics: ".gitignore should exclude .env"):

    ```
    # API key and secrets — never commit
    .env
    .env.*

    # Pi runtime logs
    *.log
    weather_lights.log

    # macOS junk
    .DS_Store

    # Python
    __pycache__/
    *.pyc
    *.pyo
    ```

    After creating the file:
    1. Stage it: `git add .gitignore`
    2. Commit it: `git commit -m "chore: add .gitignore — exclude .env, logs, pycache"`
    3. Do NOT stage any other files (not .claude/settings.local.json, not .planning/ changes)
  </action>
  <verify>
    <automated>grep -c "\.env" /Users/sayala/Documents/Pi/.gitignore</automated>
    Must return 2 or more (both `.env` and `.env.*` lines match).
    Also verify: `git log --oneline -1` shows the .gitignore commit.
  </verify>
  <acceptance_criteria>
    - `/Users/sayala/Documents/Pi/.gitignore` exists
    - `grep "^\.env$" /Users/sayala/Documents/Pi/.gitignore` returns `.env`
    - `grep "\.log" /Users/sayala/Documents/Pi/.gitignore` returns at least one match
    - `git status` shows .gitignore as tracked (not listed under untracked)
    - `git log --oneline -1` message contains "gitignore"
    - `git check-ignore -v .env` shows .gitignore is ignoring .env (if .env exists locally)
  </acceptance_criteria>
  <done>.gitignore committed to main with .env and *.log excluded, verified via git check-ignore or grep</done>
</task>

<task type="auto">
  <name>Task 2: Push all commits to github.com/saraayala1/maps</name>
  <files></files>
  <read_first>
    - Run `git log --oneline origin/main..HEAD` to see exactly what commits will be pushed
    - Run `git status` to confirm working tree is clean or only contains intentionally unstaged files
    - Run `git remote -v` to confirm remote is https://github.com/saraayala1/maps.git
  </read_first>
  <action>
    Push the main branch to origin. The local branch is already 3 commits ahead (production code) plus the new .gitignore commit from Task 1:

    ```
    git push origin main
    ```

    After push succeeds:
    1. Verify with `git log --oneline origin/main..HEAD` — output should be empty (no local commits ahead)
    2. Confirm with `git status` — shows "Your branch is up to date with 'origin/main'"

    If push is rejected (non-fast-forward): DO NOT force push. Fetch first (`git fetch origin`), inspect divergence, then report to the user.

    Do NOT push any .planning/ files, .claude/ files, or SUMMARY files in this step — only the commits already on main are pushed.
  </action>
  <verify>
    <automated>git log --oneline origin/main..HEAD 2>/dev/null | wc -l | tr -d ' '</automated>
    Must return `0` — no local commits ahead of origin after push.
  </verify>
  <acceptance_criteria>
    - `git log --oneline origin/main..HEAD` produces no output (empty — nothing left to push)
    - `git status` first line says "Your branch is up to date with 'origin/main'"
    - `git ls-remote origin main` shows the same commit hash as `git rev-parse HEAD`
    - weather_lights.py, start_weather.sh, stop_weather.sh, README.md, pi-setup.md are all present on https://github.com/saraayala1/maps (verify by running: `curl -s https://api.github.com/repos/saraayala1/maps/contents/ | python3 -c "import sys,json; files=[f['name'] for f in json.load(sys.stdin)]; print(files)"`)
    - .env does NOT appear in the file list returned by the GitHub API call above
  </acceptance_criteria>
  <done>git push succeeded; github.com/saraayala1/maps/tree/main contains all five production files and .gitignore; no .env file is present in the remote</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local filesystem → git index | Files staged for commit — .env must never cross this boundary |
| local repo → GitHub remote | Commits pushed to public repo — no secrets in any commit object |
| GitHub → public internet | Repo is public; anything committed is permanently public |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | .env / API key | mitigate | .gitignore excludes .env before push; git check-ignore verification in acceptance criteria |
| T-04-02 | Information Disclosure | git history | mitigate | Verify .env was never previously committed: `git log --all --full-history -- .env` must return empty before push |
| T-04-03 | Information Disclosure | *.log files | mitigate | .gitignore excludes *.log and weather_lights.log specifically |
| T-04-04 | Tampering | non-fast-forward push | accept | Push will be rejected by GitHub if non-fast-forward; executor is instructed not to force-push |
| T-04-05 | Information Disclosure | .planning/ internal docs | accept | .planning/ files contain no secrets (decisions, context only); acceptable to leave untracked |
</threat_model>

<verification>
After both tasks complete, run these checks:

1. `git log --oneline origin/main..HEAD` — must return empty
2. `git status` — must show "up to date with 'origin/main'"
3. `curl -s https://api.github.com/repos/saraayala1/maps/contents/ | python3 -c "import sys,json; files=[f['name'] for f in json.load(sys.stdin)]; assert '.env' not in files, '.env FOUND in repo!'; print('Safe:', files)"` — must not contain .env
4. `git log --all --full-history -- .env` — must return empty (API key never in history)
5. Open https://github.com/saraayala1/maps in browser and confirm README renders correctly
</verification>

<success_criteria>
- github.com/saraayala1/maps contains weather_lights.py, start_weather.sh, stop_weather.sh, README.md, pi-setup.md, .gitignore
- No .env file or API key appears in any commit in the repo history
- README.md is publicly readable and renders the project description and setup pointer
- REPO-01 and REPO-02 are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/04-github-repo/04-01-SUMMARY.md` using the summary template.
</output>
